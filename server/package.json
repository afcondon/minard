{
  "name": "ce-server",
  "version": "0.1.0",
  "type": "module",
  "description": "HTTP server for Code Explorer data API",
  "scripts": {
    "start": "node --enable-source-maps run.js"
  },
  "dependencies": {
    "duckdb": "^1.1.3"
  }
}
